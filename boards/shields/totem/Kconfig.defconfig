
# SPDX-License-Identifier: MIT

config ZMK_SPLIT
    default y
    depends on SHIELD_TOTEM_LEFT || SHIELD_TOTEM_RIGHT

if SHIELD_TOTEM_LEFT
    config ZMK_KEYBOARD_NAME
        default "TOTEM_L"
endif

if SHIELD_TOTEM_RIGHT
    config ZMK_KEYBOARD_NAME
        default "TOTEM_R"
endif
